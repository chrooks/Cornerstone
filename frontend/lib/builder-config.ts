/**
 * builder-config.ts — Configurable constants for the Team Builder feature.
 *
 * Adjust these values to change salary cap rules without touching component logic.
 */

/** Total salary cap budget in dollars. */
export const SALARY_CAP = 195_000_000;

/** Default salary assigned to a cornerstone legend (legends have no real salary). */
export const LEGEND_SALARY = 54_000_000;

/** Maximum number of players in a rotation. */
export const MAX_ROSTER_SLOTS = 9;
